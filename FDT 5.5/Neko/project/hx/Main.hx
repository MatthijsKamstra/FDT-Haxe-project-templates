package ${domainName}.${clientFolderName}.${projectFolderName};

class ${mainClassName} {	
	
	// constructor
	public function new(){
	}
	
	// entry point
	public static function main() {
		trace("Hello From FDT Haxe (Neko)!");
		new ${mainClassName}();
	}
}
